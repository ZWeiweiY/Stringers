using UnityEngine;

public class ChordsAndStrumming : MonoBehaviour
{
    public enum ChordsType { G, C, A, D, F}
    public ChordsType chordsType;
    public enum StrummingType { Up, Down}
    public StrummingType strummingType;
    void Awake()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
